| {% load dillo_filters %}
| {% if request.user.is_authenticated %}
a.btn.btn-block.px-3(
	href="{% if event|is_attended:request.user %}{% url 'event_attend_toggle' event.slug 'decline' %}{% else %}{% url 'event_attend_toggle' event.slug 'attend' %}{% endif %}",
	data-attend_toggle_label="{% if event|is_attended:request.user %}Going{% else %}I'm going! {% endif %}",
	class="js-attend-toggle {% if event|is_attended:request.user %}btn-primary{% else %}btn-outline-primary{% endif %}",
	)
	| {% if event|is_attended:request.user %}I'm going! {% else %}Going{% endif %}
| {% endif %}
