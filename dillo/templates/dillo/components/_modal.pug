#modal.modal.fade(tabindex='-1', role='dialog', aria-labelledby='modal-title', aria-hidden='true')
	.modal-dialog.modal-dialog-centered(role='document')
		.modal-content
			.modal-header
				h2#modal-title.modal-title Modal title
				button.close(type='button', data-dismiss='modal', aria-label='Close')
					span(aria-hidden='true')
						i.i-cancel
			.modal-body
